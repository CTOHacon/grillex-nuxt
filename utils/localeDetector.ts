// Detect based on query, cookie, header
const defineI18nLocaleDetector = (event: any, config: any) => {

    console.log('🌐 Event:', event);

    return config.defaultLocale
}

export default defineI18nLocaleDetector